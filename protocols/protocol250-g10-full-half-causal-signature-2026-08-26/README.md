# Protocol 250

Archive-only comparison of the G10 full- and half-timestep outer-tube causal signature.

- `PROTOCOL.md`: prospective observables, thresholds, gates, and claim boundary.
- `runner.py`: authenticates both parent lanes, reevaluates the tube norm twice, and publishes the
  bounded result.
- `causal_signature_core.py`: pure geometry, projection, resolution, and comparison helpers.
- `src/bhps/`: exact inherited ADM reconstruction source closure.
- `sealed-inputs/`: immutable full/half states, leaves, and passing prerequisite records.
- `candidate-output/`: absent at freeze and the only permitted result namespace.

The submitted PRD paper is outside this capsule and is not edited.
