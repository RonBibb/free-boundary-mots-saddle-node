# Protocol 250: G10 full/half causal-signature consistency

## Question

Does the resolved spacelike causal signature of the authenticated G10 outer marginal tube agree
at the same physical leaves between the independently evolved full-timestep and half-timestep
records?

Protocol 247 already established a resolved spacelike late outer tube on G9, G10, and G11.
Repeating that spatial test would add no evidence. This prospective archive-only successor instead
tests the missing independent temporal control. It performs no evolution, surface solve, parent
construction, prior-artifact mutation, or submitted-paper edit.

## Frozen records and observable

The full-timestep parent is Protocol 244 and the half-timestep parent is Protocol 240-v3. The
classification-driving centers are the nine matched physical leaves

`full steps 39--47 <-> half steps 78--94`,

with neighboring leaves retained for the backward, centered, and forward tube velocities. Both
lanes therefore use the same physical leaf spacing `3.125e-5`; only the underlying evolution
timestep differs by a factor of two.

At each center and each surface node, the normal-plane tube-tangent norm is reevaluated as

`g(V,V) - g(V,S)^2/g(S,S)`

with signature convention `(-,+,+,+,+)`. The subtraction removes tangential relabeling along the
leaf. The metric, lapse, and shift are reconstructed independently from each lane's authenticated
state. The three velocity paths are archived separately.

## Frozen acceptance

1. Protocols 240-v3, 244, 246, 247, and 249 must be authentic passing prerequisites.
2. Every copied state and leaf receipt, archive, array record, physical time, and surface admission
   record must validate exactly.
3. Full and half theta grids and matched physical times must agree exactly.
4. Every full and half leaf must preserve outward stability, negative inward expansion, marginal
   surface admission, and orthogonal-contact residual below the inherited `2e-4` ceiling.
5. At every center, all backward, centered, and forward norm estimates must have positive sign;
   the centered norm must exceed the local maximum one-sided spread at every node.
6. At every matched node and for every velocity path, the full/half norm discrepancy divided by
   `max(abs(full), abs(half), 1e-12)` must be strictly below `0.01`.
7. Protocol 247's three-grid spacelike transfer and Protocol 249's finite-segment integrated
   balance must remain admitted.
8. The complete metric reconstruction, signature evaluation, and comparison must repeat exactly.

The one-percent temporal ceiling is inherited from the already frozen Protocol 246/247 transfer
scale and is fixed before any Protocol 250 causal-norm difference is evaluated.

## Meaning

Passing yields `G10-FULL-HALF-CAUSAL-SIGNATURE-CONSISTENCY-PASS`. Together with the authenticated
three-grid geometry, stability, inward-expansion, area-growth, and integrated-balance results, a
pass establishes finite-resolution evidence for a spacelike free-boundary dynamical-horizon
segment in the selected symmetry sector and foliation.

It does not establish a continuum theorem, an event horizon, connected global topology, a global
inter-sector charge, mass transport, source ownership, or a universal result outside the selected
sector and evolution.
